export enum AppleProductId {
  PhotoPack10 = 'photo_pack_10',
  PhotoSingle = 'photo_single',
}

export enum AppleBaseProductId {
  PhotoPack10 = AppleProductId.PhotoPack10,
  PhotoSingle = AppleProductId.PhotoSingle,
}